apply plugin: "org.jlleitschuh.gradle.ktlint"

ktlint {
    android = true
    version = "0.37.0"
    ignoreFailures = false
    reporters {
        reporter "plain"
        reporter "checkstyle"
    }
    outputToConsole = true
}
