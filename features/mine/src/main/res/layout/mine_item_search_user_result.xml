<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="model"
            type="com.leevinapp.monitor.core.core.user.UserModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="@dimen/dimen_common_margin">

        <TextView
            android:id="@+id/tv_name"
            style="@style/MonitorText.Title"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:text="@{model.fullName}"
            tools:text="XXX机构名称" />

        <TextView
            android:id="@+id/tv_value"
            style="@style/MonitorText.Title"
            android:text="@{String.valueOf(model.residenceId)}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="6105001234567892" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>